import { useEffect, useState } from "react";
import { Mail, ShieldCheck, UserRound } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { getUsers } from "../services/adminService";
function AdminUsers(){const[users,setUsers]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{getUsers().then(setUsers).catch(e=>setError(e.response?.data?.message||"Unable to load users.")).finally(()=>setLoading(false));},[]);return <main className="min-h-screen bg-neutral-50 px-4 py-8 sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl"><div className="mb-8"><p className="text-xs font-semibold uppercase tracking-[0.25em] text-neutral-500">ADMIN / CUSTOMERS</p><h1 className="mt-2 text-3xl font-semibold">Users</h1><p className="mt-2 text-sm text-neutral-500">Registered Raritone customers and account roles.</p></div>{error&&<div className="mb-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<Card className="rounded-2xl"><CardHeader><CardTitle>Customer directory</CardTitle><CardDescription>{users.length} registered accounts</CardDescription></CardHeader><CardContent>{loading?<div className="space-y-3">{Array.from({length:6}).map((_,i)=><div key={i} className="h-16 animate-pulse rounded-xl bg-neutral-100"/>)}</div>:users.length===0?<div className="py-12 text-center text-sm text-neutral-500">No users found.</div>:<div className="divide-y">{users.map(u=><div key={u._id} className="flex flex-col gap-3 py-4 sm:flex-row sm:items-center"><div className="flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-full bg-neutral-100">{u.profileImage?<img src={u.profileImage} alt={u.name} className="h-full w-full object-cover"/>:<UserRound className="h-5 w-5 text-neutral-500"/>}</div><div className="min-w-0 flex-1"><p className="font-medium">{u.name}</p><p className="flex items-center gap-1 text-sm text-neutral-500"><Mail className="h-3.5 w-3.5"/>{u.email}</p></div><Badge variant={u.role==="admin"?"default":"secondary"}>{u.role==="admin"&&<ShieldCheck/>}{u.role}</Badge></div>)}</div>}</CardContent></Card></div></main>}
export default AdminUsers;
